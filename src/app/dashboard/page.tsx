import { Auth } from "@/component/auth";
import React from "react";

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <Auth />
    </div>
  );
};

export default Dashboard;
